# SD-A-01-RAG — Python dependencies
# Pinning policy: exact (==) for all top-level packages.
# Reproducibility is the project priority; transitive dependencies are
# resolved by pip but can be frozen later via `pip freeze` if drift is
# observed.
# Versions are the latest stable on PyPI as of 2026-05-09.
# Target: Python 3.11 on macOS Apple Silicon (M4), conda env `dsenv`.

# --- RAG framework ---
langchain==1.2.18
langchain-community==0.4.1
langchain-ollama==1.1.0
langchain-text-splitters==1.1.2

# --- Vector index ---
faiss-cpu==1.13.2

# --- Embeddings ---
sentence-transformers==5.4.1

# --- LLM client (Ollama Python client) ---
ollama==0.6.2

# --- PDF + tokenization ---
pypdf==6.11.0
tiktoken==0.12.0

# --- Data handling ---
numpy==2.4.4
pandas==3.0.2
pyarrow==24.0.0

# --- Visualization ---
matplotlib==3.10.9
seaborn==0.13.2

# --- Phase E.3 grading (Cohen's kappa) ---
scikit-learn==1.7.2

# --- Measurement layer ---
codecarbon==3.2.6
psutil==7.2.2
tqdm==4.67.3

# --- Notebook environment ---
jupyter==1.1.1
ipykernel==7.2.0
